# Note from `scruth-config`
This directory is managed by `scruth-config` for the purpose of a convenient place to store 1password related documents. 

Scripts in `.chezmoiscripts` may create files in this destination path.

It is not an official or "known" location of the 1password product itself, as of March 2026.
